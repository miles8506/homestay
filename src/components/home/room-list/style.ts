import styled from "styled-components"

export const RoomListWrapper = styled.div`
  display: flex;
  flex-wrap: wrap;
  margin: 0 -8px;

  .room-item-wrapper {
    flex: 25%;
    padding: 8px;
  }
`
