import styled from "styled-components"

export const SectionPlusRoomWrapper = styled.div`
  .plus-home-list {
    display: flex;
    margin: 0 -8px;
  }

  .room-item-wrapper {
    width: 20%;
    padding: 0 8px;
    flex-shrink: 0;
  }
`
